{
  "version": 2,
  "buildCommand": "npm install && npm run build",
  "startCommand": "npm run start",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.js"
    }
  ]
}